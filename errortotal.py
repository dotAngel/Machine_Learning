import errorvector as evector

ErrorTotal1 = 0

# for i in range(3):
#     ErrorTotal1 = ErrorTotal1 + evector.e[i] * evector.e[i]

ErrorTotal1 += (evector.e[0] * evector.e[0])
ErrorTotal1 += (evector.e[1] * evector.e[1])
ErrorTotal1 += (evector.e[2] * evector.e[2])


