# generar datos aleatorios y contruir un csv en base a ellos

def generate_data(count, type):
    if type == "altura&pesos":
        return
    else: return
        
    return

def save_data_to_file(filename, directory, data):
    return